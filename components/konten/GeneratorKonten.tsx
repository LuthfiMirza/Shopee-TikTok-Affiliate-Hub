'use client'
import { useState } from 'react'
import { promptGeneratorKonten } from '@/lib/prompts'
import type { GeneratorConfig, JenisKonten } from '@/types'
import { CopyButton } from '@/components/ui/CopyButton'
const jenis: JenisKonten[] = ['review','unboxing','tutorial','perbandingan','hook','caption']
const audiens = ['Remaja 17-22', 'Dewasa 23-35', 'Ibu rumah tangga', 'Pria muda']
export function GeneratorKonten() { const [config, setConfig] = useState<GeneratorConfig>({ produk: '', jenisKonten: 'review', targetAudiens: audiens[0], poinUtama: '' }); const [prompt, setPrompt] = useState(''); const [hasil, setHasil] = useState(''); const [loading, setLoading] = useState(false); const [error, setError] = useState(''); const buatPrompt = () => setPrompt(promptGeneratorKonten(config)); const generate = async () => { const nextPrompt = prompt || promptGeneratorKonten(config); setPrompt(nextPrompt); setLoading(true); setError(''); try { const res = await fetch('/api/ai', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ prompt: nextPrompt }) }); const data = await res.json(); if (!res.ok) throw new Error(data.error); setHasil(data.result) } catch (e) { setError(e instanceof Error ? e.message : 'Gagal generate') } finally { setLoading(false) } }; return <div className="space-y-4"><div className="grid gap-3 md:grid-cols-2"><input className="rounded-xl border p-3 dark:bg-gray-800" placeholder="Nama/deskripsi produk" value={config.produk} onChange={(e) => setConfig({ ...config, produk: e.target.value })} /><select className="rounded-xl border p-3 dark:bg-gray-800" value={config.jenisKonten} onChange={(e) => setConfig({ ...config, jenisKonten: e.target.value as JenisKonten })}>{jenis.map((j) => <option key={j} value={j}>{j}</option>)}</select><select className="rounded-xl border p-3 dark:bg-gray-800" value={config.targetAudiens} onChange={(e) => setConfig({ ...config, targetAudiens: e.target.value })}>{audiens.map((a) => <option key={a}>{a}</option>)}</select><textarea className="rounded-xl border p-3 dark:bg-gray-800" placeholder="Poin utama (opsional)" value={config.poinUtama} onChange={(e) => setConfig({ ...config, poinUtama: e.target.value })} /></div><div className="flex gap-2"><button onClick={buatPrompt} className="rounded-xl border px-4 py-2 font-semibold">Buat Prompt</button><button onClick={generate} className="rounded-xl bg-primary px-4 py-2 font-semibold text-white">Generate dengan AI</button></div>{prompt && <div className="space-y-2"><pre className="whitespace-pre-wrap rounded-xl bg-gray-100 p-4 text-sm dark:bg-gray-800">{prompt}</pre><CopyButton text={prompt} size="sm" /></div>}{loading && <div className="h-24 animate-pulse rounded-xl bg-gray-100 dark:bg-gray-800" />}{error && <p className="text-sm text-red-600">{error}. Jika belum punya API key, gunakan prompt copy-paste.</p>}{hasil && <div className="space-y-2"><div className="whitespace-pre-wrap rounded-xl border p-4 dark:border-gray-700">{hasil}</div><CopyButton text={hasil} size="sm" /></div>}</div> }
