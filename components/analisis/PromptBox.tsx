'use client'
import { useMemo, useState } from 'react'
import { promptAnalisisProduk } from '@/lib/prompts'
import { useAnalisisStore } from '@/store/useAnalisisStore'
import { CopyButton } from '@/components/ui/CopyButton'
export function PromptBox() { const produk = useAnalisisStore((s) => s.produk); const prompt = useMemo(() => produk ? promptAnalisisProduk(produk) : '', [produk]); const [loading, setLoading] = useState(false); const [result, setResult] = useState(''); const ask = async () => { setLoading(true); setResult(''); try { const res = await fetch('/api/ai', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ prompt }) }); const data = await res.json(); setResult(data.result || data.error) } finally { setLoading(false) } }; if (!produk) return <p className="text-sm text-gray-500">Prompt AI akan muncul setelah analisis.</p>; return <div className="space-y-3"><pre className="whitespace-pre-wrap rounded-xl bg-gray-100 p-4 text-sm dark:bg-gray-800">{prompt}</pre><div className="flex flex-wrap gap-2"><CopyButton text={prompt} /><button onClick={ask} className="rounded-lg border px-3 py-2 text-sm font-medium hover:bg-gray-50 dark:border-gray-700 dark:hover:bg-gray-800">Tanya AI Langsung</button></div>{loading && <div className="h-24 animate-pulse rounded-xl bg-gray-100 dark:bg-gray-800" />}{result && <div className="whitespace-pre-wrap rounded-xl border p-4 text-sm dark:border-gray-700">{result}</div>}<p className="text-xs text-gray-500">Jika API key belum tersedia, copy prompt ini ke ChatGPT/Claude gratis secara manual.</p></div> }
